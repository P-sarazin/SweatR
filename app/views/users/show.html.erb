
<div class="my-container">
  <div class="two-columns">
    <h3>Your progress:</h3>
  </div>
  <div class="two-columns">
    <h3>Your next classes:</h3>
      <div class="sessions-card">
        <% @lessons.each do |lesson| %>
          <div class="col-xs-12 col-md-4">
            <%= render "card", lesson: lesson, user: lesson.user %>
          </div>
        <% end %>
      </div>


  </div>
</div>

